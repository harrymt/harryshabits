{
  "name": "arthur-messenger-bot",
  "version": "1.0.28",
  "description": "",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/harrymt/habit-reward-chatbot"
  },
  "engines": {
    "node": "6.9.4"
  },
  "scripts": {
    "test": "node tests/main.js",
    "start": "node index.js",
    "deploy": "npm --no-git-tag-version version patch && make && now -e EMAIL_PASS=@email_pass -e EMAIL_ID=@email_id -e FB_PAGE_TOKEN=@fb_page_token -e FB_VERIFY_TOKEN=@fb_verify_token -e USER_ID=@user_id -e AIRTABLE_API_KEY=@airtable_api_key -e CRON_SECRET=@cron_secret --public && now-realias && npm run logs",
    "logs": "now logs arthur-messenger-bot"
  },
  "homepage": "https://github.com/harrymt/habit-reward-chatbot",
  "bugs": "https://github.com/harrymt/habit-reward-chatbot/issues",
  "author": "Harry Mumford-Turner",
  "license": "MIT",
  "dependencies": {
    "airtable": "^0.5.0",
    "body-parser": "^1.15.0",
    "dotenv": "^4.0.0",
    "express": "^4.13.4",
    "nodemailer": "^4.0.1",
    "now-logs": "0.0.7",
    "request": "^2.71.0",
    "request-promise": "^4.2.1",
    "sqlite3": "^3.1.8"
  },
  "xo": {
    "space": true
  }
}
